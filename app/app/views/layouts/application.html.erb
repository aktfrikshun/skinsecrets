<!DOCTYPE html>
<html class="scroll-smooth">
  <head>
    <title>Olga's Skin Secrets - Professional Esthetician Services</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= favicon_link_tag "skin_secrets.jpeg" %>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    
    <!-- Font Awesome for icons -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    
    <!-- Google Fonts - Beautiful typography for health and beauty -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:ital,wght@0,400;0,500;0,600;0,700;1,400&family=Inter:wght@300;400;500;600;700&family=Cormorant+Garamond:ital,wght@0,300;0,400;0,500;0,600;1,400&family=Poppins:wght@300;400;500;600;700&family=Montserrat:wght@300;400;500;600;700&family=Lora:ital,wght@0,400;0,500;0,600;1,400&family=Merriweather:ital,wght@0,300;0,400;0,700;1,400&family=Source+Sans+Pro:wght@300;400;600;700&display=swap" rel="stylesheet">
  </head>

  <body class="font-poppins bg-gray-50 text-gray-900 antialiased leading-relaxed">
    <!-- Navigation -->
    <nav class="fixed top-0 left-0 right-0 z-50 bg-white/95 backdrop-blur-md border-b border-gray-200/50 shadow-sm">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between items-center h-16">
          <!-- Logo -->
          <a href="<%= root_path %>" class="flex items-center space-x-3 group">
            <%= image_tag "skin_secrets.jpeg", class: "w-10 h-10 rounded-full object-cover shadow-lg", alt: "Skin Secrets Logo" %>
            <span class="font-playfair text-lg font-semibold text-amber-800 tracking-wide">OLGA'S SKIN SECRETS</span>
          </a>
          
          <!-- Desktop Navigation -->
          <div class="hidden md:flex items-center space-x-8">
            <a href="<%= root_path %>" class="text-gray-700 hover:text-amber-600 transition-colors duration-200 font-medium">Home</a>
            <a href="<%= services_path %>" class="text-gray-700 hover:text-amber-600 transition-colors duration-200 font-medium">Services</a>
            <a href="<%= about_path %>" class="text-gray-700 hover:text-amber-600 transition-colors duration-200 font-medium">About</a>
            <a href="<%= contact_path %>" class="text-gray-700 hover:text-amber-600 transition-colors duration-200 font-medium">Contact</a>
            <a href="<%= forum_path %>" class="text-gray-700 hover:text-amber-600 transition-colors duration-200 font-medium">Community</a>
          </div>
          
          <!-- User Menu -->
          <div class="hidden md:flex items-center space-x-4">
            <% if logged_in? %>
              <a href="<%= appointments_path %>" class="text-gray-700 hover:text-amber-600 transition-colors duration-200 font-medium">
                <i class="fas fa-calendar-alt mr-2"></i>My Appointments
              </a>
              <div class="relative group">
                <button class="flex items-center space-x-2 text-gray-700 hover:text-amber-600 transition-colors duration-200 font-medium">
                  <i class="fas fa-user"></i>
                  <span><%= @current_user.first_name %></span>
                  <i class="fas fa-chevron-down text-xs"></i>
                </button>
                <div class="absolute right-0 mt-2 w-48 bg-white rounded-lg shadow-lg border border-gray-200 opacity-0 invisible group-hover:opacity-100 group-hover:visible transition-all duration-200">
                  <a href="<%= profile_path %>" class="block px-4 py-2 text-gray-700 hover:bg-amber-50 hover:text-amber-600 transition-colors duration-200">Profile</a>
                  <% if @current_user&.email == 'admin@skinsecretsnc.com' %>
                    <a href="<%= admin_dashboard_path %>" class="block px-4 py-2 text-gray-700 hover:bg-amber-50 hover:text-amber-600 transition-colors duration-200">
                      <i class="fas fa-cog mr-2"></i>Admin
                    </a>
                  <% end %>
                  <hr class="border-gray-200">
                  <%= button_to "Logout", logout_path, method: :delete, class: "w-full text-left px-4 py-2 text-gray-700 hover:bg-amber-50 hover:text-amber-600 transition-colors duration-200", form: { style: "display: inline;" } %>
                </div>
              </div>
            <% else %>
              <a href="<%= login_path %>" class="text-gray-700 hover:text-amber-600 transition-colors duration-200 font-medium">Login</a>
              <a href="<%= signup_path %>" class="bg-gradient-to-r from-amber-500 to-amber-600 text-white px-6 py-2 rounded-full font-medium hover:from-amber-600 hover:to-amber-700 transition-all duration-200 shadow-md hover:shadow-lg transform hover:-translate-y-0.5">Sign Up</a>
            <% end %>
          </div>
          
          <!-- Mobile menu button -->
          <button class="md:hidden p-2 rounded-md text-gray-700 hover:text-amber-600 hover:bg-gray-100 transition-colors duration-200" id="mobile-menu-button">
            <i class="fas fa-bars text-xl"></i>
          </button>
        </div>
        
        <!-- Mobile Navigation -->
        <div class="md:hidden hidden" id="mobile-menu">
          <div class="px-2 pt-2 pb-3 space-y-1 border-t border-gray-200">
            <a href="<%= root_path %>" class="block px-3 py-2 text-gray-700 hover:text-amber-600 hover:bg-gray-50 rounded-md transition-colors duration-200">Home</a>
            <a href="<%= services_path %>" class="block px-3 py-2 text-gray-700 hover:text-amber-600 hover:bg-gray-50 rounded-md transition-colors duration-200">Services</a>
            <a href="<%= about_path %>" class="block px-3 py-2 text-gray-700 hover:text-amber-600 hover:bg-gray-50 rounded-md transition-colors duration-200">About</a>
            <a href="<%= contact_path %>" class="block px-3 py-2 text-gray-700 hover:text-amber-600 hover:bg-gray-50 rounded-md transition-colors duration-200">Contact</a>
            <a href="<%= forum_path %>" class="block px-3 py-2 text-gray-700 hover:text-amber-600 hover:bg-gray-50 rounded-md transition-colors duration-200">Community</a>
            <% if logged_in? %>
              <a href="<%= appointments_path %>" class="block px-3 py-2 text-gray-700 hover:text-amber-600 hover:bg-gray-50 rounded-md transition-colors duration-200">
                <i class="fas fa-calendar-alt mr-2"></i>My Appointments
              </a>
              <a href="<%= profile_path %>" class="block px-3 py-2 text-gray-700 hover:text-amber-600 hover:bg-gray-50 rounded-md transition-colors duration-200">Profile</a>
              <% if @current_user&.email == 'admin@skinsecretsnc.com' %>
                <a href="<%= admin_dashboard_path %>" class="block px-3 py-2 text-gray-700 hover:text-amber-600 hover:bg-gray-50 rounded-md transition-colors duration-200">
                  <i class="fas fa-cog mr-2"></i>Admin
                </a>
              <% end %>
              <%= button_to "Logout", logout_path, method: :delete, class: "w-full text-left px-3 py-2 text-gray-700 hover:text-amber-600 hover:bg-gray-50 rounded-md transition-colors duration-200", form: { style: "display: inline;" } %>
            <% else %>
              <a href="<%= login_path %>" class="block px-3 py-2 text-gray-700 hover:text-amber-600 hover:bg-gray-50 rounded-md transition-colors duration-200">Login</a>
              <a href="<%= signup_path %>" class="block px-3 py-2 bg-gradient-to-r from-amber-500 to-amber-600 text-white rounded-md font-medium">Sign Up</a>
            <% end %>
          </div>
        </div>
      </div>
    </nav>

    <!-- Flash Messages -->
    <% if notice %>
      <div class="fixed top-20 left-1/2 transform -translate-x-1/2 z-50 bg-green-50 border border-green-200 text-green-800 px-6 py-3 rounded-lg shadow-lg max-w-md mx-4" id="flash-notice">
        <div class="flex items-center justify-between">
          <div class="flex items-center">
            <i class="fas fa-check-circle mr-2"></i>
            <span><%= notice %></span>
          </div>
          <button onclick="this.parentElement.parentElement.remove()" class="text-green-600 hover:text-green-800 ml-4">
            <i class="fas fa-times"></i>
          </button>
        </div>
      </div>
    <% end %>
    
    <% if alert %>
      <div class="fixed top-20 left-1/2 transform -translate-x-1/2 z-50 bg-red-50 border border-red-200 text-red-800 px-6 py-3 rounded-lg shadow-lg max-w-md mx-4" id="flash-alert">
        <div class="flex items-center justify-between">
          <div class="flex items-center">
            <i class="fas fa-exclamation-circle mr-2"></i>
            <span><%= alert %></span>
          </div>
          <button onclick="this.parentElement.parentElement.remove()" class="text-red-600 hover:text-red-800 ml-4">
            <i class="fas fa-times"></i>
          </button>
        </div>
      </div>
    <% end %>

    <!-- Main Content -->
    <main class="pt-16">
      <%= yield %>
    </main>

    <!-- Footer -->
    <footer class="bg-gray-900 text-white">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
        <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
          <!-- Brand -->
          <div>
            <div class="flex items-center space-x-3 mb-4">
              <%= image_tag "skin_secrets.jpeg", class: "w-10 h-10 rounded-full object-cover", alt: "Skin Secrets Logo" %>
              <h5 class="font-playfair text-xl font-semibold text-amber-400">OLGA'S SKIN SECRETS</h5>
            </div>
            <p class="text-gray-400 mb-4">Professional esthetician services for radiant, healthy skin. Your beauty journey starts here with personalized care.</p>
            <div class="flex space-x-4">
              <a href="https://www.facebook.com/profile.php?id=61563299254618" target="_blank" rel="noopener noreferrer" class="text-gray-400 hover:text-amber-400 transition-colors duration-200">
                <i class="fab fa-facebook-f text-xl"></i>
              </a>
              <a href="#" class="text-gray-400 hover:text-amber-400 transition-colors duration-200">
                <i class="fab fa-instagram text-xl"></i>
              </a>
              <a href="#" class="text-gray-400 hover:text-amber-400 transition-colors duration-200">
                <i class="fab fa-twitter text-xl"></i>
              </a>
            </div>
          </div>
          
          <!-- Quick Links -->
          <div>
            <h6 class="text-amber-400 font-semibold mb-4">Quick Links</h6>
            <ul class="space-y-2">
              <li><a href="<%= root_path %>" class="text-gray-400 hover:text-amber-400 transition-colors duration-200">Home</a></li>
              <li><a href="<%= services_path %>" class="text-gray-400 hover:text-amber-400 transition-colors duration-200">Services</a></li>
              <li><a href="<%= about_path %>" class="text-gray-400 hover:text-amber-400 transition-colors duration-200">About</a></li>
              <li><a href="<%= contact_path %>" class="text-gray-400 hover:text-amber-400 transition-colors duration-200">Contact</a></li>
              <li><a href="<%= forum_path %>" class="text-gray-400 hover:text-amber-400 transition-colors duration-200">Community</a></li>
            </ul>
          </div>
          
          <!-- Contact Info -->
          <div>
            <h6 class="text-amber-400 font-semibold mb-4">Contact Info</h6>
            <div class="space-y-3">
              <div class="flex items-start space-x-3">
                <i class="fas fa-map-marker-alt text-amber-400 mt-1"></i>
                <div>
                  <p class="text-gray-400">110 Nyon Rd</p>
                  <p class="text-gray-400">New Bern, NC 28562</p>
                </div>
              </div>
              <div class="flex items-center space-x-3">
                <i class="fas fa-phone text-amber-400"></i>
                <p class="text-gray-400">(919) 897-0150</p>
              </div>
              <div class="flex items-center space-x-3">
                <i class="fas fa-envelope text-amber-400"></i>
                <p class="text-gray-400">skinsecretsnc@gmail.com</p>
              </div>
            </div>
          </div>
        </div>
        
        <hr class="border-gray-800 my-8">
        
        <div class="flex flex-col md:flex-row justify-between items-center">
          <p class="text-gray-400">&copy; 2024 Olga's Skin Secrets. All rights reserved.</p>
          <p class="text-gray-400">Privacy Policy | Terms of Service</p>
        </div>
      </div>
    </footer>

    <!-- Mobile Menu JavaScript -->
    <script>
      document.getElementById('mobile-menu-button').addEventListener('click', function() {
        const mobileMenu = document.getElementById('mobile-menu');
        mobileMenu.classList.toggle('hidden');
      });
    </script>
  </body>
</html>
